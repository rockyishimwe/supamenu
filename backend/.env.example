# MongoDB connection string
MONGO_URI=mongodb://localhost:27017/dineflow

# JWT secret key for authentication tokens (change in production!)
JWT_SECRET=your_secret_here

# Backend server port
PORT=5000

# Allowed CORS origin (frontend URL)
CORS_ORIGIN=http://localhost:3000

# SMTP mail transport (used for email verification & password resets)
# For Gmail: create an app password, use smtp.gmail.com, port 587
# For SendGrid: use smtp.sendgrid.net, port 587, user=apikey
# For Mailtrap (dev testing): use sandbox.smtp.mailtrap.io, port 2525
SMTP_HOST=smtp.ethereal.email
SMTP_PORT=587
SMTP_USER=mariussangwa@gmail.com
SMTP_PASS=msnz lghq gode rbvr
MAIL_FROM=noreply@dineflow.app
